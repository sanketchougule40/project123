pipeline {
    agent any
    tools {
        jdk 'java'
        maven 'maven'
    }
    stages {
        stage('Test Java') {
            steps {
                sh 'java -version'
            }
        }
    }
}
